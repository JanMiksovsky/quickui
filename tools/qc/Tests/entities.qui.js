//
// Entities
//
Entities = QuickUI.Control.extend({
	className: "Entities"
});
$.extend(Entities.prototype, {
  ready: function() {
    if (2 < 3 && 4 < 5)
    {
      alert("Success");
    }
  }
});

