<Control name="Greet">
	
<prototype>
	Hello,
	<span id="Greet_content" class="enabled"/><span id="Greet_punctuation">.</span>
</prototype>

<style>
{
	font-family: Arial, Helvetica, sans serif;
	font-size: 11pt;
}

#Greet_content.enabled {
	cursor: pointer;
}
</style>

<script>
$.extend(Greet.prototype, {
	
	content: QuickUI.Element("Greet_content").content(),
	enabled: QuickUI.Element("Greet_content").applyClass("enabled"),
	punctuation: QuickUI.Element("Greet_punctuation").content(),
	text: QuickUI.Element("Greet_content").text(),

	ready: function() {
		var self = this;
		$(this.Greet_content).click(function() {
			if (self.enabled()) {
				var newName = prompt("Enter name:", self.text());
				if (newName != null) {
					self.text(escape(newName));
				}
			}
		});
	}
});
</script>

</Control>